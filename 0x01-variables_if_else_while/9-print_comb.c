#include <stdio.h>
/**
 * main - entry point
 *
 * Description: prints all possibele single digit
 * Return: returns 0
 */
int main(void)
{
	int n = '0';
	int c = 44
	while (n >= '0' && n <= '9')
	{
		putchar(n);
		putchar(c);
		putchar(' ');
		n++;
	}
return (0);
}
